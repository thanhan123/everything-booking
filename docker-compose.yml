version: "3.9"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: deps
    command: sh -c "npm install && npm run dev"
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
